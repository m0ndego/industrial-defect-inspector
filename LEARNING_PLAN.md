# 10 天学习执行表

每天按“目标 → 直觉 → 最少知识 → 实现 → 验证 → 练习 → 复盘”进行。代码已经提供工程主干，
但标注为“亲手”的任务需要你自己修改、运行并解释；不要只读答案。

## Day 1｜环境和仓库

- 目标：分清 Python、虚拟环境、依赖和 Git 仓库。
- 实现：用 `uv sync --extra cuda --extra dev` 还原环境。
- 验证：打印 Python、PyTorch、CUDA 和 GPU 名称。
- 亲手：故意在系统 Python 3.14 中导入 PyTorch，比较错误与项目环境。
- 复盘：解释为什么虚拟环境不应提交到 Git。

## Day 2｜数据管线

- 目标：理解训练、校准和测试数据的职责。
- 实现：运行 `prepare-data`，阅读 `manifest.json`。
- 验证：确认训练与校准路径没有交集，缺陷图都有掩码。
- 亲手：改变 seed，比较两份 manifest；再改回 42。
- 复盘：解释为什么阈值不能使用测试标签。

## Day 3｜卷积自编码器

- 目标：理解编码、压缩、解码和重建误差。
- 实现：逐层打印输入输出 shape。
- 验证：`ConvAutoencoder` 输入输出均为 `(N, 3, H, W)`。
- 亲手：暂时删除一个上采样层，阅读并解释 shape 错误，然后恢复。
- 复盘：画出三次下采样和三次上采样。

## Day 4｜训练与阈值

- 目标：完成 PyTorch 的前向、损失、反向和更新闭环。
- 实现：训练自编码器并评估。
- 验证：损失为有限数值，生成 `metrics.json` 和结果图。
- 亲手：把学习率改大 100 倍，记录现象，再恢复默认值。
- 复盘：解释“训练损失下降”为什么不等于“缺陷检测一定更好”。

## Day 5｜阅读开源仓库

- 目标：沿调用路径阅读，而不是从第一行读到最后一行。
- 阅读顺序：CLI → `train_patchcore` → Anomalib `Engine.fit` → `Patchcore` → memory bank。
- 验证：能说明数据、特征、coreset 和最近邻分数在哪里产生。
- 亲手：在纸上画出调用链，并给每个节点写一句输入/输出。
- 复盘：列出自己代码与 Anomalib 分别承担的职责。

## Day 6｜PatchCore

- 目标：理解预训练特征和正常样本记忆库。
- 实现：训练、评估 PatchCore。
- 验证：checkpoint 可重新加载，同一图片可重复预测。
- 亲手：比较 coreset ratio 0.01 与 0.1 的指标、时间和模型大小。
- 复盘：解释 PatchCore 为什么没有常规反向传播训练。

## Day 7｜产品化推理

- 目标：从“实验代码”走到“可被别人使用的命令”。
- 实现：对一张正常图、一张缺陷图和一个目录运行 `predict`。
- 验证：JSON 字段完整，热力图与叠加图能定位缺陷。
- 亲手：传入不存在的路径和损坏图片，记录错误信息。
- 复盘：说明异常分数、阈值和最终判断的关系。

## Day 8｜测试与 CI

- 目标：让错误能被自动发现。
- 实现：运行 Ruff、普通测试和 PatchCore 集成测试。
- 验证：普通测试无需数据和预训练权重；集成测试只在显式开启时运行。
- 亲手：制造一次训练/校准数据重叠，让测试失败，然后恢复。
- 复盘：区分单元测试、smoke test 与 GPU 集成测试。

```powershell
$env:RUN_PATCHCORE_SMOKE = "1"
uv run pytest tests/test_patchcore_smoke.py
```

## Day 9｜复现与文档

- 目标：让陌生人只看 README 也能运行。
- 实现：从干净环境依次执行数据准备、训练、评估和推理。
- 验证：把两份 `metrics.json` 的实测值填入 README，不手抄猜测值。
- 亲手：写一个成功案例、一个失败案例和一个局限。
- 复盘：用三分钟讲清业务问题、方案、指标和改进方向。

## Day 10｜GitHub 协作与简历

- 目标：留下可信的工程证据，而不是一次性上传文件。
- 实现：检查 diff、提交、推送；创建 issue、feature branch 和 draft PR。
- 验证：远端默认分支正确、CI 通过、仓库不含数据/权重/密钥。
- 亲手：自己写 PR 描述，包含 Why、What、Validation、Limitations。
- 复盘：只把真实实测指标写入简历，并准备解释一个排错案例。

## 阶段验收问题

完成后应能不用看答案解释：

1. 为什么只用正常图片训练？
2. 校准集和测试集为什么不能混用？
3. 自编码器和 PatchCore 的异常分数分别从哪里来？
4. image AUROC、pixel AUROC 和 F1 各回答什么问题？
5. 如果换成新的产品图片，哪些部分必须重新训练或校准？
